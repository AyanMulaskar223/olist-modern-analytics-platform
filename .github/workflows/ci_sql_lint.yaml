name: CI - SQL Linting (Fast)

on:
  push:
    branches: ['feat/**', 'fix/**', 'staging']
  pull_request:
    branches: ['main', 'staging']

jobs:
  lint-sql:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install SQLFluff + dbt
        run: |
          pip install sqlfluff==3.0.0 sqlfluff-templater-dbt==3.0.0 dbt-snowflake==1.11.0

      # 🔧 FIX: Set working directory for dbt
      - name: Install dbt Dependencies
        working-directory: ./03_dbt
        run: dbt deps

      # 🔧 FIX: Correct path to models directory
      - name: Lint SQL (with dbt context)
        working-directory: ./03_dbt
        run: |
          sqlfluff lint models/ --dialect snowflake --templater dbt
