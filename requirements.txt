# =======================================================
# 🚀 Olist Modern Analytics Platform - Python Dependencies
# =======================================================

# -------------------------------------------------------
# 1. CORE TRANSFORMATION ENGINE (dbt)
# -------------------------------------------------------
# We only install the adapter. It automatically pulls the matching dbt-core.
# Using ~=1.8.0 ensures you get stability (v1.8.x) without breaking changes.
dbt-snowflake~=1.8.0

# -------------------------------------------------------
# 2. CODE QUALITY & LINTING (The "Guardrails")
# -------------------------------------------------------
# SQLFluff enforces your SQL style guide.
# The templater is required to understand dbt macros {{ ref() }}.
sqlfluff==3.0.7
sqlfluff-templater-dbt==3.0.7

# -------------------------------------------------------
# 3. AUTOMATION & GIT HOOKS
# -------------------------------------------------------
# Runs checks automatically before you commit (detects secrets, trailing spaces).
# You have a .pre-commit-config.yaml, so this is MANDATORY.
pre-commit==3.7.1

# -------------------------------------------------------
# 4. DATA UTILITIES (For Local Debugging/EDA)
# -------------------------------------------------------
# Essential for inspecting CSV seeds or running small Python analyses.
pandas>=2.0.0
pyarrow>=14.0.0  # Required for modern Pandas/Snowflake performance
python-dotenv>=1.0.0 # Helps manage .env secrets safely

# -------------------------------------------------------
# 5. TESTING (Optional but Recommended)
# -------------------------------------------------------
# If you write custom Python scripts, use this to test them.
pytest>=8.0.0

# -------------------------------------------------------
# 6. DOCUMENTATION (MkDocs + Theme + Plugins)
# -------------------------------------------------------
# Enables local docs server and GitHub Pages deployment.
mkdocs==1.6.1
mkdocs-material==9.7.1
mkdocs-awesome-pages-plugin==2.10.1
mkdocs-glightbox==0.5.2
pymdown-extensions==10.20.1
